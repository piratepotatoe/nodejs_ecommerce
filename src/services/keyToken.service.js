'use strict';

class KeyTokenService{
    static createKeyToke = async ({user, publicKey}) =>{
        try {
            const publicKeyString = publicKey.toString();
        } catch (error) {
            
        }
    }
}
module.exports = KeyTokenService;